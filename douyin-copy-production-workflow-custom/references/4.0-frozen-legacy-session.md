# 4.0：3.5换芯设计 → 固定旧2.5副本扩写

用户于2026-09-08认可随机第11题测试结果，并明确命名为4.0。用户说“4.0”“4.0批量换芯扩写”“先批量换芯再用旧2.5”时采用本模式。4.0是生产流程版本，不是Gemini模型名称。未指定版本时不要擅自替换其他版本默认值。

## 最高优先级：每篇独立复制同一基准

**每篇都从同一份旧2.5备份复制一个会话，再放入一篇换芯稿。** 不得连续向一个会话投放多篇；不得拿上一篇生成后的会话作为下一篇的基准。批次之间也继续使用同一基准。原旧会话和基準都不传给生成命令、不追加消息、不更新。

基准（Mac）：`/Users/kin/Documents/Codex/2026-07-02/gemini/work/4.0_legacy_baseline/session.json`

SHA256：`30c4109b440815e5c1c668d188ae72723f9eef99ac4fee5e49ae5377e8fbfb3c`

原始备份：`/Users/kin/Documents/Codex/2026-09-08/ban/outputs/旧2.5会话备份/gemini_session_25_legacy_20260908_114627_random_test.json`

基准和原始备份内容相同，基准已设为只读。会话内容不加入skills Git仓库。若基准丢失，可由上述备份恢复并核对同一哈希；两者均缺失时报告缺失，不能用当前活跃旧会话、3.5会话、空白会话或某篇成稿会话替代。Windows没有此基准时优先在Mac执行；迁移时复制同一文件并核对哈希。

## 换芯阶段

采用3.5的内容设计方法：锁定母题、受众承诺与数量，设计候选路线，比较同母题历史及本批已生成路线，选择新的判断/因果/证明路线，再用知识库补充支持。知识用于支撑，不能直接拼卡片当观点。情感相关性保持到各核心点。

每篇冻结约500—1000个中文字的短稿，包含核心判断、简洁因果、必要场景方向；不要提前写成长篇成稿。具体承诺数量以用户及选题为准。批量稿件需有机制和场景差异；初次规模建议5篇，不是固定数量门槛。未获事实支持的案例写作假设场景，不能强制造成有姓名、有成功结果的“真实Gary学员”。

这里只继承3.5的内容规划、知识支持和重复检查方法。**不继承3.5扩写提示词、长度/节奏锁、开头包装、强制案例、验收修辞改写或会话。** 不调用 `build_35_gemini_prompt.py` 生成4.0请求。

## 扩写阶段

固定提示词位于同目录 `4.0-old25-prompt.txt`，来自本次成功测试的完整旧2.5直接出稿块，包含原文开始标记。组装方式为该完整块＋本篇冻结稿＋原文结束标记。不得用后来更新的2.5提示词静默替换。不得额外添加3.5、母题精确锁、自然节奏锁或语气修复提示。观点与必要事实边界放在冻结稿中。

用 `scripts/prepare_40_session.py --source <本篇冻结稿> --work-dir <本篇全新目录>` 准备。本工具只校验和复制，不调用模型；拒绝已存在目录、错误基准哈希或被改动的固定提示词。输出本篇 `session.json`、`prompt.txt`、`source.txt` 与来源清单。

在现有Mac Gemini runner目录 `/Users/kin/Documents/Codex/2026-07-02/gemini` 执行：

```sh
./scripts/run_gemini_chat.sh --prompt-file <本篇目录/prompt.txt> --session-file <本篇目录/session.json> --output-file <本篇目录/raw.txt>
```

只传本篇工作副本。不要用 `--isolated`、`--new-session`、混合默认会话或共享的批次会话。不得绕开runner原有上下文压缩机制；固定的是各篇输入前的历史起点，不承诺服务端行为和模型输出永远一致。

通常一次扩写。空响应或网络失败，可从同一基准另建本篇重试目录，用相同冻结稿重试一次；再次失败报告原因。不因没有达到旧提示词6000—8000字偏好自动补写或重跑。严重截断、跑题或缺少主要论点需要返工时记录原因，仍从固定基准另建副本；成稿不喂给下一篇。

## 检查与交付

保留Gemini原文。只检查主旨和论点完整性、明显重复、虚构真实案例及事实/科学失真；这是问题定位，不是3.5文风验收。不得自动改成温和稿、压成3.5篇幅、追加开头或归一化旧结尾。事实问题需要修订时另存修订版与变更说明，保留原稿；不要静默覆盖用户认可的语气。

Word忠实呈现原文，原始开头和CTA保留；可用简洁文档标题标明主题和版本，无需三版开头、标题包或案例黄标。问题说明放在单独文件，不混入正文。保留本篇冻结稿、实际提示词、原始输出和来源清单。交付前校验Word正文与raw一致，按文档技能检查版式，并再次校验基准哈希。

记录在独立4.0批次清单：题目、母题、所选机制、场景、稿件路径、基准哈希、源稿哈希、生成状态和用户反馈。生成、用户认可、排期和发布分别记录。不要污染其他版本的提示词、验收规则或生产状态；内容查重可参考跨版本历史。

## 已认可样本

《女人为什么会对某些男人身体上头：雄性吸引力的四大神经机制全剖析》，788字换芯稿一次扩成5051字、40段、6页Word。用户评价“效果很好”。这证明用户认可此次语气与流程，不能推导成科学表述均正确或已有播放效果。
