# 花研Ryan隔离契约

## 不可共享的状态

以下内容不得与探花Gary共用或互相覆盖：

- Gemini会话文件和历史摘要；
- 母题、标题、机制、案例、换芯、换新和查重台账；
- 提示词输出、临时稿、Word成品、TTS稿和验收报告；
- `CURRENT_CONTEXT.md`、任务清单和失败重试记录；
- CTA品牌、固定片尾和咨询案例身份。

## 可以只读复用的能力

- Gemini命令行执行器；
- DOCX导出库；
- TTS/SRT技术检查器；
- 通用文本统计、哈希和文件格式验证代码；
- 已确认的开头结构方法，但不得复用男性标题台账或成稿文本。

## 路径白名单

花研Ryan生产写入只允许发生在：

- `/Users/kin/Documents/Codex/花研Ryan工作台/`
- `/Users/kin/工作用（同步）/花研Ryan/`
- 当前任务明确建立的临时目录

技能安装更新只允许写入：

- `/Users/kin/.codex/skills/huayan-ryan-copy-workflows/`

## 停止条件

出现任一情况立即停止：

- 写入目标不在女性白名单；
- 男性技能目录的前后哈希发生变化；
- 女性产物出现 `探花Gary`、`Gary哥`、`兄弟们`、`哥们`、`男性学员` 等身份残留；
- 调用男性Gemini会话或男性台账；
- 准备覆盖已经存在且来源不明的文件；
- 试运行路线尚未获用户确认却准备批量生成。

停止后保留现场和报告，不自动修复男性文件，不自动重跑付费模型。

